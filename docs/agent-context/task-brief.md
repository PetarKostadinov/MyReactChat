# Optional task brief

Use this only for larger work. A normal prompt is enough for small fixes.

```text
Goal:

User-visible result:

In scope:

Out of scope:

Constraints or compatibility requirements:

Acceptance checks:

Relevant screenshots, errors, or links:
```

If fields are omitted, inspect the repository and make conservative, reversible assumptions.

