# Local development uses the Create React App proxy from package.json.
# Keep these empty so a production API URL from .env is not baked into npm start.
REACT_APP_API_URL=
REACT_APP_SOCKET_URL=http://localhost:5000
